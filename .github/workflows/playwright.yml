name: Playwright Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:
    # Allow manual trigger from GitHub Actions UI

# The dependency audit lives in its own workflow (`.github/workflows/audit.yml`)
# so that a newly-published advisory reports as its own check rather than turning
# this suite red for a reason unrelated to any test.
jobs:
  unit-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Run Unit Tests with Coverage
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7

  e2e-tests:
    runs-on: ${{ matrix.runs-on }}
    timeout-minutes: 15
    needs: unit-tests
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}
      TEST_ENV: prod

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Chromium Browsers
            runs-on: ubuntu-24.04
            projectFlags: --project=chromium --project=chrome --project=edge --project=mobile-chrome
            browsers: chromium chrome msedge
            browser-engine: chromium
          - name: Firefox
            runs-on: ubuntu-24.04
            projectFlags: --project=firefox
            browsers: firefox
            browser-engine: firefox
          - name: WebKit Browsers
            runs-on: macos-latest
            projectFlags: --project=webkit --project=mobile-safari --project=tablet
            browsers: webkit
            browser-engine: webkit

    name: E2E — ${{ matrix.name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/ms-playwright
            ~/Library/Caches/ms-playwright
          key: playwright-${{ runner.os }}-${{ matrix.browser-engine }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-${{ matrix.browser-engine }}-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps ${{ matrix.browsers }}

      - name: Install Playwright system dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps ${{ matrix.browser-engine }}

      - name: Run Playwright tests
        run: npx playwright test ${{ matrix.projectFlags }}

      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report-${{ matrix.name }}
          path: playwright-report/
          retention-days: 7

      # Aggregated healing metrics for this shard — success rate, average heal
      # time, token spend, and per-provider breakdown. Written by HealingReporter
      # from the per-worker shards. Uploaded separately from the HTML report so
      # it can be downloaded and diffed without unpacking the full report.
      - name: Upload healing report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: healing-report-${{ matrix.name }}
          path: test-results/healing-report.json
          if-no-files-found: ignore
          retention-days: 7
